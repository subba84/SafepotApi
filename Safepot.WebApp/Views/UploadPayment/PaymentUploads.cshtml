@model IEnumerable<Safepot.Entity.SfpPaymentUpload>
@{
    ViewData["Title"] = "Payment Uploads";
}

<main id="main" class="main">

    <div class="pagetitle">
        <h1>Users</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="@Url.Action("Index","Dashboard")">Home</a></li>
                <li class="breadcrumb-item active">Payment Uploads</li>
            </ol>
        </nav>
    </div><!-- End Page Title -->

    <section class="section dashboard">
        <div class="row">

            <div class="col-lg-12">

                <div class="card">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-lg-6">
                                <h5 class="card-title">Payment Uploads</h5>
                            </div>
                            <div class="col-lg-6">
                                <input type="button" class="btn btn-primary" style="float:right;margin-top:5px;" value="+ Upload Payment Details" onclick="fnGotoPaymentUpload();" />
                            </div>
                        </div>

                        <!-- Table with stripped rows -->
                        <table class="table datatable">
                            <thead>
                                <tr>
                                    <th scope="col">#</th>
                                    <th scope="col">Agent Name</th>
                                    <th scope="col">Phone</th>
                                    <th scope="col">Company</th>
                                    <th scope="col">Delete</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model != null && Model.Count() > 0)
                                {
                                    int i = 1;
                                    foreach (var user in Model)
                                    {
                                        <tr>
                                            <th scope="row">@i</th>
                                            <td>@(user.AgentName)</td>
                                            <td>@user.MobileNumber</td>
                                            <td>@user.CompanyName</td>
                                            <td style="text-align:center">
                                                <a href="@Url.Action("Delete","UploadPayment")?id=@user.Id" style="color:red">
                                                    <i b-mk0rxp7ouj="" class="bi bi-trash"></i>
                                                </a>
                                            </td>
                                        </tr>
                                        i++;
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="5">No Data Found</td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                        <!-- End Table with stripped rows -->

                    </div>
                </div>

            </div>

        </div>
    </section>
</main>

<script src="~/js/jquery-3.4.1.min.js"></script>
<script type="text/javascript">
    function fnGotoPaymentUpload() {
        window.location.href = "@Url.Action("UploadPayment", "UploadPayment")";
    };
</script>


